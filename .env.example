# ===== 电商实时数据大屏 - 环境变量说明 =====
# 复制此文件为 .env 或 .env.local 并修改对应值
# Vite 环境变量必须以 VITE_ 开头才会暴露给客户端代码

# API 基础路径，开发时可指向本地 mock server 或后端开发环境
VITE_API_BASE_URL=/api

# 应用标题，显示在浏览器标签页和页面标题栏
VITE_APP_TITLE=电商实时数据大屏

# 是否启用 Mock 数据模式（true=使用本地 mock，false=调用真实 API）
VITE_USE_MOCK=true
